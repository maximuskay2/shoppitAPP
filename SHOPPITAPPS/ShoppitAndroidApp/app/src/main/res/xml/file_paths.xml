<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow sharing from cache/images directory -->
    <files-path
        name="images"
        path="images/" />

    <!-- Optional: allow from general cache if needed -->
    <cache-path
        name="cache"
        path="." />

    <!-- Optional: external files (if you save to external storage) -->
    <!-- <external-files-path name="external_files" path="." /> -->
</paths>